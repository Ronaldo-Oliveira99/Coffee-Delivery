import styled from 'styled-components'

export const SelectedCoffeeContainer = styled.div`
  display: flex;
  gap: 1.25rem;
  padding: 0.5rem;

  img {
    height: 4rem;
    width: 4rem;
  }
  label {
    width: 100%;
    text-align: end;
    font-weight: 900;
  }
`
export const SelectedCoffeeButtonsContainer = styled.div`
  display: flex;
  gap: 0.5rem;
  margin-top: 0.5rem;
`
